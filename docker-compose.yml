version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: nexspaces_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: api
    ports:
      - "8080:8080"
    environment:
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASSWORD=postgres
      - DB_NAME=nexspaces_dev
      - DB_SSLMODE=disable
      - REDIS_URL=redis://redis:6379/0
      - ENVIRONMENT=development
      - JWT_SECRET=dev-secret-change-in-production
      - ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
      # Payment
      - PAYMENT_DEFAULT_PROVIDER=iyzico
      - PAYMENT_IYZICO_ENABLED=true
      - PAYMENT_IYZICO_API_KEY=sandbox-test-key
      - PAYMENT_IYZICO_SECRET_KEY=sandbox-test-secret
      - PAYMENT_IYZICO_BASE_URL=https://sandbox-api.iyzipay.com
      - PAYMENT_CHECKOUT_ENABLED=true
      - PAYMENT_CHECKOUT_PUBLIC_KEY=pk_test_key
      - PAYMENT_CHECKOUT_SECRET_KEY=sk_test_secret
      - PAYMENT_CHECKOUT_BASE_URL=https://api.sandbox.checkout.com
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
